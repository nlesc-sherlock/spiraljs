/*jslint nomen:true */
/*globals describe, it, spiral, expect, d3, _ */

describe('the suite of tests', function () {

    'use strict';

    it('...should do stuff', function () {

        var actual,
            expected;

        console.log(d3);
        console.log(_);
        console.log(spiral.BubbleSpiral);

        actual = true;
        expected = true;

        expect(actual).toEqual(expected);

    });

});
